[package]
name = "random-translate"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.9"
translators = { version = "0.1", features = ["google"] }
clap = { version = "4.5.30", features = ["derive"] }
